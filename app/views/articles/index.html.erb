<%= link_to "Add a new Post", new_article_path, class: "pull-right btn btn-primary" %>
<h1>Outreach Blog</h1>
<% @articles.each do |article| %>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <p>
    <%= article.body %>
  </p>
<% end %>
